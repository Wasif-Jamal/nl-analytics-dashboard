# Tasks: SQL Agent Subagent Refactor

Derived from `proposal.md` and `plan.md`.
Branch: `refactor/sql-agent-subagent`.
Commit only after Phase 3 checkpoint (all gates green).

---

## Phase 1 — Foundation

Create the `app/tools/` package. Nothing existing is touched; all gates stay green.

- [x] **1.1** Create `app/tools/__init__.py` — empty package init
- [x] **1.2** Create `app/tools/sql_tools.py` — `SqlTools` class
  - Constructor: `__init__(self, llm, api_base_url: str)`
  - `generate_sql` closure: `@tool`, `(question: str) -> SQLGenerationOutput`, calls `llm.with_structured_output(SQLGenerationOutput).invoke([SystemMessage(SQL_SYSTEM_PROMPT), HumanMessage(question)])`
  - `validate_sql` closure: `@tool`, `(sql: str) -> dict`, calls `validate_select_only`, returns `{"valid": True}` or `{"valid": False, "reason": "..."}`
  - `execute_sql` closure: `@tool`, `(sql: str, explanation: str, tool_call_id: Annotated[str, InjectedToolCallId]) -> Command`, defense-in-depth validate → `httpx.Client.post(f"{api_base_url}/api/query")` → write `WorkflowState` via `Command`
  - `handle_unidentifiable` closure: `@tool`, `(tool_call_id: Annotated[str, InjectedToolCallId]) -> Command`, writes `error_message=_ERR_UNIDENTIFIED`, `query_result=None`, `generated_sql=None`
  - Expose all four as instance attributes: `self.generate_sql = generate_sql` etc.
  - Module docstring + `SqlTools` class docstring + all `@tool` docstrings

### Checkpoint 1
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: all green (no existing code touched).

---

## Phase 2 — Core Implementation

Rewrite the agent, prompts, and graph. Prompt tasks are independent; run in parallel.
Pytest will break after this phase — fixed in Phase 3.

- [x] **2.1** [PARALLEL] Update `app/prompts/sql_prompt.py`
  - Replace three-step `validate_and_execute` workflow with four-tool workflow:
    ```
    STEP 1 — Call generate_sql(question) → get sql, explanation, is_identifiable
    STEP 2 — If is_identifiable is false, call handle_unidentifiable() and stop
    STEP 3 — Call validate_sql(sql); if valid=false, retry from STEP 1 with error context
    STEP 4 — Call execute_sql(sql, explanation) to execute and store results;
              if it fails, correct and retry from STEP 1 with the error context
    ```
  - Keep DATABASE SCHEMA and SQL RULES sections unchanged
  - Update module docstring

- [x] **2.2** [PARALLEL] Update `app/prompts/orchestrator_prompt.py`
  - Replace `query_database` tool reference with `transfer_to_sql_agent` handoff tool
    (auto-generated by `create_supervisor` from `name="sql_agent"`)
  - New body:
    ```
    You are an analytics supervisor. A business user asks a question about the data,
    and you coordinate agents to answer it.

    You have one agent available:
    - transfer_to_sql_agent: routes the question to the SQL Agent, which generates,
      validates, and executes the corresponding SQL query.

    Instructions:
    1. For any data question, call transfer_to_sql_agent exactly once.
    2. After the SQL Agent returns, do not call it again. Provide a brief closing
       message — the application reads the structured result from state.
    3. Never attempt to write or fabricate SQL, data, or analysis yourself.
    ```
  - Update module docstring

- [x] **2.3** Rewrite `app/agents/sql_agent.py`
  - Remove: `SqlAgentState`, `_build_validate_and_execute()`, `get_tools()`
  - Remove imports: `AgentState`, `BaseTool` (no longer needed)
  - Add import: `from app.tools.sql_tools import SqlTools`
  - `__init__` signature unchanged: `(self, llm, api_base_url=None, retry_limit=None)`
  - Build `SqlTools(llm=llm, api_base_url=self._api_base_url)` in `__init__`
  - `self._agent = create_agent(model=llm, tools=[sql_tools.generate_sql, sql_tools.validate_sql, sql_tools.execute_sql, sql_tools.handle_unidentifiable], system_prompt=SQL_SYSTEM_PROMPT, state_schema=WorkflowState, name="sql_agent").with_config({"recursion_limit": self._retry_limit * 2 + 1})`
  - Preserve `_api_base_url`, `_retry_limit` storage, logging
  - Update module docstring (remove `query_database`/`get_tools` references)

- [x] **2.4** Rewrite `app/orchestration/graph.py`
  - Add import: `from langgraph_supervisor import create_supervisor`
  - Remove import: `create_agent` (no longer used in this module)
  - `build()`: replace `create_agent(tools=sql_agent.get_tools(), ...)` with
    `create_supervisor(agents=[sql_agent._agent], model=self._llm, prompt=ORCHESTRATOR_PROMPT, state_schema=WorkflowState).compile()`
  - Remove `tools = sql_agent.get_tools()` / `tool_names` log lines; add subagent log
  - Update class and `build()` docstrings
  - Return type `CompiledStateGraph` is unchanged

### Checkpoint 2
```bash
uv run ruff check .
uv run ruff format --check .
```
`pytest` is expected to fail here — old tests reference removed APIs (`get_tools`, `query_database`, `SqlAgentState`). Tests are fixed in Phase 3.

---

## Phase 3 — Tests

One test per spec scenario. Keep `_QueryServiceTransport` and DB fixtures intact.

### 3.1 Rewrite `tests/agents/test_sql_agent.py`

Tests cover `SqlTools` tools directly (unit level, no inner agent mock needed).

- [x] **3.1.1** `test_generate_sql_identifiable` — `generate_sql.func(question=...)` with mocked `llm.with_structured_output` returning `is_identifiable=True`; assert returns `SQLGenerationOutput` with `is_identifiable=True`
  - Spec: `natural-language-to-sql / question maps to known entities` + `sql-tools-class / tools capture injected deps`

- [x] **3.1.2** `test_generate_sql_unidentifiable` — same mock, `is_identifiable=False`; assert `is_identifiable=False`
  - Spec: `natural-language-to-sql / question references unknown entities`

- [x] **3.1.3** `test_validate_sql_valid_select` — `validate_sql.func(sql="SELECT 1")`; assert `{"valid": True}`
  - Spec: `read-only-validation / valid SELECT query`

- [x] **3.1.4** `test_validate_sql_rejects_write` — `validate_sql.func(sql="DROP TABLE orders")`; assert `valid=False` and `"reason" in result`
  - Spec: `read-only-validation / write or DDL statement generated`

- [x] **3.1.5** `test_execute_sql_success` — mock `httpx.Client` returning 1-row response; assert `query_result.row_count==1`, `error_message=None`, `generated_sql==sql`, `sql_explanation==explanation`, `messages[0].content` matches template `"retrieved 1 rows. Columns: ..."`
  - Spec: `sql-execution / query returns rows` + `tool-message-summary / successful execution` + `execute-sql-terminal / successful execution writes all result fields`

- [x] **3.1.6** `test_execute_sql_zero_rows` — mock returning 0-row response; assert `error_message=_ERR_EMPTY`, `query_result=None`
  - Spec: `sql-execution / query returns zero rows`

- [x] **3.1.7** `test_execute_sql_defense_in_depth_blocks_write` — call with `sql="DELETE FROM orders"` (no mock); assert `error_message=_ERR_VALIDATION`, no HTTP call
  - Spec: `execute-sql-terminal / defense-in-depth validation fails inside execute_sql`

- [x] **3.1.8** `test_execute_sql_http_error` (parametrized: `httpx.RequestError`, `httpx.HTTPStatusError`) — mock `httpx.Client.post` raising; assert `error_message=_ERR_DATABASE`
  - Spec: `sql-execution / database error`

- [x] **3.1.9** `test_handle_unidentifiable` — `handle_unidentifiable.func(tool_call_id="tc1")`; assert `error_message=_ERR_UNIDENTIFIED`, `query_result=None`, `generated_sql=None`, `messages[0].content==_ERR_UNIDENTIFIED`
  - Spec: `handle-unidentifiable / unidentifiable question triggers handle_unidentifiable`

- [x] **3.1.10** `test_retry_limit_sourced_from_settings` — monkeypatch `settings.sql_retry_limit=5`; build `SqlAgent`; assert `_retry_limit==5`
  - Spec: `env-configuration / SQL_RETRY_LIMIT is set`

- [x] **3.1.11** `test_retry_limit_defaults_to_settings_value` — build `SqlAgent` without explicit limit; assert `_retry_limit==settings.sql_retry_limit` (default 3)
  - Spec: `env-configuration / SQL_RETRY_LIMIT is absent`

- [x] **3.1.12** `test_sql_tools_passed_to_create_agent` — build `SqlAgent`; assert `self._agent` is not None (smoke — confirms create_agent received the tools without error)
  - Spec: `sql-tools-class / tools passed directly to create_agent`

### 3.2 Update `tests/workflows/test_sql_pipeline.py`

Keep `_QueryServiceTransport`, `_make_test_client`, `_REVENUE_SQL`, `EXPECTED_COUNTS` fixture usage.
Remove `_ScriptedInnerAgent`, `_pipeline()`, `_identifiable()`, `_run()` helpers.
Add `_make_tools()` and `_run_execute()` helpers (see plan).

- [x] **3.2.1** `test_happy_path_populates_result` — `execute_sql.func` with real DB; assert `error_message=None`, `query_result.row_count==2`, correct columns, summary message matches template
  - Spec: `sql-execution / query returns rows` + `natural-language-to-sql / generated SQL is recorded`

- [x] **3.2.2** `test_unknown_entities_skips_database` — `handle_unidentifiable.func`; assert `error_message=_ERR_UNIDENTIFIED`, `query_result=None` (no DB touched)
  - Spec: `natural-language-to-sql / question references unknown entities`

- [x] **3.2.3** `test_execution_retry_recovers` — first call `execute_sql.func(bad_sql)` → DB error; second call `execute_sql.func(good_sql)` → success; assert final `error_message=None`
  - Spec: `sql-self-correction-retry / execution fails due to bad SQL then succeeds within retry limit`

- [x] **3.2.4** `test_validation_failure_then_recovers` — `validate_sql.func("DELETE ...")` → `valid=False`; then `execute_sql.func(good_sql)` → success; assert success
  - Spec: `sql-self-correction-retry / validation fails then succeeds within retry limit`

- [x] **3.2.5** `test_all_attempts_fail_surfaces_validation_error` — three `validate_sql.func` calls all return `valid=False`; assert `error_message=_ERR_VALIDATION`, DB row count unchanged
  - Spec: `sql-self-correction-retry / all retries exhausted`

- [x] **3.2.6** `test_read_only_guard_blocks_write` — `execute_sql.func("DELETE FROM orders")`; assert `error_message=_ERR_VALIDATION`, DB intact
  - Spec: `read-only-validation / write or DDL statement generated` (integration level)

- [x] **3.2.7** `test_database_error_maps_to_retrieve_message` — `execute_sql.func` with nonexistent-column SQL; assert `error_message=_ERR_DATABASE`
  - Spec: `sql-execution / database error` (integration level)

- [x] **3.2.8** `test_empty_result_maps_to_no_data` — `execute_sql.func` with `WHERE customer_id='NONE'`; assert `error_message=_ERR_EMPTY`
  - Spec: `sql-execution / query returns zero rows` (integration level)

### 3.3 Update `tests/orchestration/test_graph.py`

- [x] **3.3.1** Keep `test_build_returns_compiled_graph` — update node set if needed (supervisor adds `sql_agent` node)
  - Spec: `sql-agent-subagent-pattern / supervisor routes to SQL Agent`

- [x] **3.3.2** Replace `test_query_database_is_registered` with `test_sql_agent_is_registered_as_subagent` — `assert "sql_agent" in graph.nodes`
  - Spec: `sql-agent-subagent-pattern / no query_database tool in supervisor graph`

- [x] **3.3.3** Add `test_no_query_database_in_supervisor` — `assert "query_database" not in set(graph.nodes["tools"].bound.tools_by_name)`
  - Spec: `query-database-tool / query_database no longer exists` (REMOVED requirement)

- [x] **3.3.4** Add `test_database_access_boundary` — assert `execute_sql` is not accessible from the supervisor's tool registry (only inside `sql_agent` subgraph)
  - Spec: `database-access-boundary / execute_sql calls the query API`

### Checkpoint 3
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: all green. This is the gate before committing.

---

## Phase 4 — Commit

- [x] **4.1** Confirm all quality gates pass (Checkpoint 3 must be green)
- [x] **4.2** Stage and commit on `refactor/sql-agent-subagent` branch

Commit message format (CLAUDE.md §5):
```
refactor(sql-agent): replace query_database tool with create_supervisor subagent pattern

Rewrites SqlAgent as a create_agent() subagent with four explicit internal tools
(generate_sql, validate_sql, execute_sql, handle_unidentifiable) and replaces the
flat-tool supervisor with create_supervisor([sql_agent._agent]). Establishes the
canonical agent pattern for all four agents in the system.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
